name: Demo (11)

on:
  workflow_dispatch:
    inputs:
      artifactid:
        description: "Artifact ID"
        required: false
        default: "100"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (none)
        run: echo "Skipping checkout (Azure pipeline had: checkout: none)"

      - name: Param value
        run: echo "Param value ${{ inputs.artifactid }}"

      - name: Run a multi-line script
        run: |
          echo "Add other tasks to build, test, and deploy your project."
          echo "See https://aka.ms/yaml"